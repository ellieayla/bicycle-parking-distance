[project]
name = "bicycle-parking-distance"
version = "0.1.0"
description = "Find the distance to nearest bicycle parking for a collection of OSM POIs"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "geojson>=3.2.0",
    "geopy>=2.4.1",
    "progress>=1.6.1",
]
